import React, { useContext } from "react";
import { Row, Col,Card } from "antd";

const ListData = ({}) => {
const onChannge = () => {

}
    return(
        <Row>
            <Col span={8} offset={8}>
              
            </Col>
        </Row>
    )
}
export default React.memo(ListData)